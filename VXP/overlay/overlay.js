const themes={usa250:{title:'USA250',sub:'Liberty Celebration',badge:'AMERICA 250',a:'#B23A48',b:'#172D5A',c:'#F6C653'},webexone:{title:'WebexOne',sub:'Drone Show Activated',badge:'AUSTIN 2026',a:'#FFD400',b:'#07111f',c:'#39D2FF'},flgisa:{title:'FLGISA',sub:'Florida Hero',badge:'FLORIDA HERO',a:'#2ECC71',b:'#07351F',c:'#F6C653'},mlb:{title:'MLB',sub:'Game Day Experience',badge:'GAME DAY',a:'#2E86DE',b:'#061A33',c:'#F6C653'},nba:{title:'NBA',sub:'All-Star Experience',badge:'ALL STAR',a:'#C8102E',b:'#101A33',c:'#F6C653'}};
const $=id=>document.getElementById(id);
const canvas=$('overlayCanvas'),ctx=canvas.getContext('2d');
const nameInput=$('nameInput'),eventInput=$('eventInput'),imageInput=$('imageInput'),fileInput=$('fileInput');
const renderBtn=$('renderBtn'),downloadBtn=$('downloadBtn'),statusEl=$('status'),themeInfo=$('themeInfo');
let currentDataUrl=null, uploadedUrl=null;
function params(){return new URLSearchParams(location.search)}
function clean(s,f=''){return String(s||f).trim()}
function setStatus(msg,cls=''){statusEl.textContent=msg;statusEl.className='status '+cls}
function theme(){return themes[eventInput.value]||themes.usa250}
function updateThemeInfo(){const t=theme();themeInfo.innerHTML=`<div class="theme-chip"><b>${t.title}</b><span>${t.sub}<br>${t.badge}</span><div class="swatch" style="background:linear-gradient(135deg,${t.a},${t.b});box-shadow:inset 0 0 0 2px ${t.c}55"></div></div>`}
function roundRect(x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath()}
function fitText(text,max,fontBase,min=34){let size=fontBase;do{ctx.font=`900 ${size}px Inter, Arial, sans-serif`;if(ctx.measureText(text).width<=max)break;size-=3}while(size>min);return size}
function drawCover(img,x,y,w,h){const ir=img.width/img.height, r=w/h;let sx=0,sy=0,sw=img.width,sh=img.height;if(ir>r){sw=img.height*r;sx=(img.width-sw)/2}else{sh=img.width/r;sy=(img.height-sh)/2}ctx.drawImage(img,sx,sy,sw,sh,x,y,w,h)}
function drawSampleArt(t){const g=ctx.createLinearGradient(0,0,1080,1620);g.addColorStop(0,t.b);g.addColorStop(.5,'#132a46');g.addColorStop(1,'#02050b');ctx.fillStyle=g;ctx.fillRect(0,0,1080,1620);for(let i=0;i<70;i++){ctx.fillStyle=`rgba(255,255,255,${Math.random()*.35})`;ctx.beginPath();ctx.arc(Math.random()*1080,Math.random()*1120,Math.random()*2.5+1,0,Math.PI*2);ctx.fill()}ctx.fillStyle=t.a+'33';ctx.beginPath();ctx.arc(540,710,330,0,Math.PI*2);ctx.fill();ctx.fillStyle=t.c;ctx.font='900 90px Georgia, serif';ctx.textAlign='center';ctx.fillText(t.title,540,650);ctx.font='700 38px Inter, Arial';ctx.fillStyle='rgba(255,255,255,.8)';ctx.fillText('Paste an image URL or upload a test image',540,725)}
function drawFrame(img){const t=theme(),name=clean(nameInput.value,'Guest Name').toUpperCase();const nick=clean(params().get('nick')||params().get('nickname'),'');const company=clean(params().get('company'),'');ctx.clearRect(0,0,1080,1620);if(img)drawCover(img,0,0,1080,1620);else drawSampleArt(t);let grd=ctx.createLinearGradient(0,0,0,350);grd.addColorStop(0,'rgba(0,0,0,.82)');grd.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=grd;ctx.fillRect(0,0,1080,360);let bottom=ctx.createLinearGradient(0,1180,0,1620);bottom.addColorStop(0,'rgba(0,0,0,0)');bottom.addColorStop(.55,'rgba(0,0,0,.78)');bottom.addColorStop(1,'rgba(0,0,0,.92)');ctx.fillStyle=bottom;ctx.fillRect(0,1120,1080,500);ctx.strokeStyle=t.c;ctx.lineWidth=10;roundRect(42,42,996,1536,34);ctx.stroke();ctx.strokeStyle=t.a;ctx.lineWidth=3;roundRect(66,66,948,1488,24);ctx.stroke();ctx.fillStyle='rgba(0,0,0,.58)';roundRect(90,92,900,162,20);ctx.fill();ctx.strokeStyle=t.c;ctx.lineWidth=2;roundRect(90,92,900,162,20);ctx.stroke();const fs=fitText(name,820,68,34);ctx.font=`900 ${fs}px Inter, Arial, sans-serif`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillStyle='#fff';ctx.shadowColor='rgba(0,0,0,.65)';ctx.shadowBlur=18;ctx.fillText(name,540,174);ctx.shadowBlur=0;if(nick){ctx.font='700 34px Georgia, serif';ctx.fillStyle=t.c;ctx.fillText('“'+nick+'”',540,232)}ctx.fillStyle='rgba(0,0,0,.62)';roundRect(110,1340,860,145,24);ctx.fill();ctx.strokeStyle=t.a;ctx.lineWidth=3;roundRect(110,1340,860,145,24);ctx.stroke();ctx.fillStyle=t.c;ctx.font='900 34px Inter, Arial';ctx.fillText(t.badge,540,1388);ctx.fillStyle='#fff';ctx.font='800 42px Inter, Arial';ctx.fillText(t.title+' · '+t.sub,540,1438);if(company){ctx.fillStyle='rgba(255,255,255,.78)';ctx.font='700 30px Inter, Arial';ctx.fillText(company,540,1482)}ctx.fillStyle=t.a;ctx.beginPath();ctx.arc(112,112,18,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(968,112,18,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(112,1508,18,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(968,1508,18,0,Math.PI*2);ctx.fill();ctx.fillStyle='rgba(255,255,255,.82)';ctx.font='800 22px Inter, Arial';ctx.textAlign='left';ctx.fillText('VEYTEC EXPERIENCE PLATFORM',90,1540);ctx.textAlign='right';ctx.fillText('POWERED BY WEBEX',990,1540);currentDataUrl=canvas.toDataURL('image/jpeg',.92);downloadBtn.disabled=false;setStatus('Overlay rendered. Download is ready.','ok')}
function loadImage(src){return new Promise((resolve,reject)=>{if(!src)return resolve(null);const img=new Image();img.crossOrigin='anonymous';img.onload=()=>resolve(img);img.onerror=()=>reject(new Error('Image could not load. Try file upload if Airtable URL blocks canvas.'));img.src=src})}
async function render(){try{setStatus('Rendering overlay...');const src=uploadedUrl||clean(imageInput.value);const img=await loadImage(src);drawFrame(img)}catch(e){console.error(e);drawFrame(null);setStatus(e.message,'err')}}
function download(){if(!currentDataUrl)return;const a=document.createElement('a');const n=clean(nameInput.value,'vxp').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');a.href=currentDataUrl;a.download=`vxp-${eventInput.value}-${n||'collectible'}.jpg`;a.click()}
function boot(){const p=params();if(p.get('name'))nameInput.value=p.get('name');if(p.get('event')&&themes[p.get('event').toLowerCase()])eventInput.value=p.get('event').toLowerCase();if(p.get('image'))imageInput.value=p.get('image');updateThemeInfo();renderBtn.addEventListener('click',render);downloadBtn.addEventListener('click',download);eventInput.addEventListener('change',()=>{updateThemeInfo();render()});nameInput.addEventListener('input',()=>render());imageInput.addEventListener('change',()=>{uploadedUrl=null;render()});fileInput.addEventListener('change',()=>{const f=fileInput.files&&fileInput.files[0];if(!f)return;if(uploadedUrl)URL.revokeObjectURL(uploadedUrl);uploadedUrl=URL.createObjectURL(f);render()});drawFrame(null);if(imageInput.value)render()}
boot();
